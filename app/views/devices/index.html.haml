%h2 Listing devices
%legend
- if @devices.empty?
  .no_device
    There are no devices in the database at the moment. Go to
    = link_to 'create page', new_device_path
    to add a device.
- else
  = render 'devices/list'
%br/
= link_to 'New Device', new_device_path, class: 'btn'
